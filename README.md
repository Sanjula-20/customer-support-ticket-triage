# customer-support-ticket-triage
A rule-based Customer Support Ticket Triage System built using Python and Google Colab. The system classifies support tickets, assigns priority levels (P0–P3), calculates SLA deadlines, and generates a manager-ready CSV output.
